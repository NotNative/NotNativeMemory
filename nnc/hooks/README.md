# NotNativeCoder Hooks

Three hooks that extend NotNativeCoder sessions with memory awareness, mirroring the Claude Code hook set but adapted to NNC's config shape, tool names, and payload format.

## Why three hooks?

Each fires at a different moment and serves a different purpose:

| Hook | NNC event | Purpose |
|------|-----------|---------|
| `user_prompt_inject.py` | `UserPromptSubmit` | prime the whole turn with context relevant to the user's request |
| `memory_inject.py` | `PreToolUse` | surface action-specific gotchas right before `edit_file`, `write_file`, `read_file`, `bash` |
| `compact_guard.py` | `PreCompact` | preserve critical rules + top memories through context compression |

The `UserPromptSubmit` hook catches decisions at the moment they get framed (when the user states intent). The `PreToolUse` hook catches actions in flight. Together they cover both phases of a turn.

## What they do

### `user_prompt_inject.py` (UserPromptSubmit)

Fires when the user sends a message. Uses the prompt text as a semantic query and injects top matches as `additionalContext` so cross-cutting preferences, past decisions, and domain-level gotchas are in scope before the model starts reasoning.

Skips trivial prompts under 15 characters to avoid noise on "ok" / "yes" / "continue." Uses a higher similarity floor than `PreToolUse` to filter weak matches (user messages are longer and noisier than tool arguments).

### `memory_inject.py` (PreToolUse)

Fires before `edit_file`, `write_file`, `read_file`, and `bash`. Builds a semantic query from the tool's arguments — file extension hints, path segment keywords, command keywords — and returns matching memories as `additionalContext`.

Two similarity tiers: memories tagged `importance=high` or `critical` surface at a lower threshold because their load-bearing status outweighs imperfect semantic matches.

Writes a telemetry line to `~/.nnc/memory_hook.log` per invocation. Failures never block the tool call.

### `compact_guard.py` (PreCompact)

Fires right before NotNativeCoder compresses the context window. Injects a short set of critical rules plus the top high-importance memories for the current project, so operational discipline survives compaction. Customize `_CRITICAL_RULES` at the top of the file for your workflow.

## Configuration

All three hooks read from `nnc/hooks/hooks.env` (auto-generated by the installer):

```
MEMORY_MCP_URL=http://localhost:9500/mcp

# PreToolUse (memory_inject.py)
MEMORY_HOOK_THRESHOLD=0.55
MEMORY_HOOK_HIGH_THRESHOLD=0.40
MEMORY_HOOK_MAX_RESULTS=3

# UserPromptSubmit (user_prompt_inject.py)
MEMORY_PROMPT_THRESHOLD=0.45
MEMORY_PROMPT_HIGH_THRESHOLD=0.35
MEMORY_PROMPT_MAX_RESULTS=3
MEMORY_PROMPT_MIN_CHARS=15

# PreCompact (compact_guard.py)
MEMORY_COMPACT_MAX_RESULTS=5
```

All values can be overridden by setting the corresponding environment variable before NotNativeCoder launches.

## Installation

Run the installer with the absolute path to your NotNativeMemory checkout:

```bash
python nnc/hooks/merge_hooks.py /absolute/path/to/NotNativeMemory http://localhost:9500/mcp
```

That idempotently merges the three hook entries into `~/.nnc/settings.json` and writes `nnc/hooks/hooks.env` with your MCP URL. Safe to re-run — updates existing entries in place rather than duplicating.

After installing, the resulting `~/.nnc/settings.json` will contain (shape simplified):

```json
{
  "hooks": {
    "UserPromptSubmit": [
      { "command": "python /.../nnc/hooks/user_prompt_inject.py", "timeout_ms": 10000 }
    ],
    "PreToolUse": [
      {
        "command": "python /.../nnc/hooks/memory_inject.py",
        "matcher": "edit_file|write_file|read_file|bash",
        "timeout_ms": 10000
      }
    ],
    "PreCompact": [
      { "command": "python /.../nnc/hooks/compact_guard.py", "timeout_ms": 10000 }
    ]
  }
}
```

## NNC payload format (for reference)

The hooks read JSON from stdin. NNC's payload shapes:

- **UserPromptSubmit**: `{"hook_event_name": "UserPromptSubmit", "prompt": "..."}`
- **PreToolUse**: `{"hook_event_name": "PreToolUse", "tool_name": "...", "tool_input": {...}, "tool_input_json": "..."}`
- **PreCompact**: `{"hook_event_name": "PreCompact", "reason": "..."}`

Tool names in the NNC ecosystem: `read_file`, `write_file`, `edit_file`, `bash`. Tool inputs use a `path` key for file operations and `command` for bash.

## What gets injected

**UserPromptSubmit output** (if matches above threshold):
```json
{
  "hookSpecificOutput": {
    "hookEventName": "UserPromptSubmit",
    "additionalContext": "[Memory Hook] Context from previous sessions relevant to this request:\n  1. [high|global|0.58] ...\n  2. ..."
  }
}
```

**PreToolUse output** (if matches above threshold):
```json
{
  "hookSpecificOutput": {
    "hookEventName": "PreToolUse",
    "additionalContext": "[Memory Hook] Relevant memories from previous sessions:\n  1. [high|0.67] ...\n  2. ..."
  }
}
```

**PreCompact output** (always, since critical rules always inject):
```json
{
  "hookSpecificOutput": {
    "hookEventName": "PreCompact",
    "additionalContext": "[Compact Guard] Critical rules preserved across context compaction:\n- ...\n\n[Compact Guard] High-priority memories from previous sessions:\n- ..."
  }
}
```

All hooks exit with code `0` on success and `1` on non-fatal error (the tool call / prompt / compaction proceeds regardless).
