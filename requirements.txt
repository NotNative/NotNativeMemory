# NotNativeMemory - MCP Memory Server
#
# Exact versions pinned for reproducible installs. Quarterly review:
# run `pip list --outdated`, check CVEs (https://osv.dev, `pip-audit`),
# bump here, re-run the test suite. Major-version bumps get a commit
# of their own with the upgrade notes.

mcp==1.26.0
asyncpg==0.31.0
sentence-transformers==3.4.1
transformers==4.57.6
python-dotenv==1.2.1
numpy==2.4.2

# Web GUI (Jinja templates + Starlette form parsing). Pulled in
# transitively by Starlette / FastMCP today; pinned explicitly so a
# clean install is reproducible regardless of upstream changes.
jinja2==3.1.6
python-multipart==0.0.22

# Observability: in-memory metric counters/histograms/gauges and the
# Prometheus exposition format for the /metrics endpoint. Small, pure
# Python, no native build.
prometheus-client==0.23.1
